/**
 * WHAT PEOPLE SAY — edit via /admin or src/content/testimonials.json,
 * not this file. `title` and `url` are optional.
 */
import data from "../content/testimonials.json";

export const TESTIMONIALS = data.items;
