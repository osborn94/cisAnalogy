/**
 * RECOMMENDED RESOURCES — edit via /admin or
 * src/content/resources.json, not this file. `disclosure` is
 * optional: use it to note any affiliate, commercial, or
 * collaborative relationship; leave it null if there isn't one.
 */
import data from "../content/resources.json";

export const RESOURCES = data.items;
